[phases.setup]
nixPkgs = ["maven", "openjdk17"]

[phases.install]
cmds = ["mvn clean package -DskipTests"]

[phases.build]
cmds = ["echo 'Build completed'"]

[start]
cmd = "java -Dspring.profiles.active=prod -Dserver.port=${PORT:-8080} -jar target/E_Commerce-0.0.1-SNAPSHOT.jar"